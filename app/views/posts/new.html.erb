<%= form_with(model: @post) do |form| %>

<% if @post.errors.any?%>
<% @post.errors.full_mesages.each do |mesage| %>
    <%= message %>
    <% end %>
<% end %>

<%= form.label :title %>
<%= form.text_field :title %>

<%= form.label :body %>
<%= form.text_field :body %>

<%= form.submit %>

<% end %>
